{
  "name": "canetti-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && npx prisma generate --schema=prisma/schema.prisma",
    "start": "cd backend && npx prisma db push && npm start"
  }
}
