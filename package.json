{
  "name": "canetti-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install --ignore-scripts && npm --prefix frontend install --ignore-scripts",
    "build": "npm --prefix frontend install --include=dev --ignore-scripts && npm --prefix backend install --include=dev --ignore-scripts && npm --prefix frontend run build && npm --prefix backend run prisma:generate && npm --prefix backend run build",
    "start": "cd backend && npx prisma migrate deploy && npm start"
  }
}

