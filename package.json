{
  "name": "canetti-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install --ignore-scripts && npm --prefix frontend install --ignore-scripts",
    "build": "npm --prefix frontend install --ignore-scripts && npm --prefix backend install --ignore-scripts && npm --prefix frontend run build && npm --prefix backend exec prisma generate && npm --prefix backend run build",
    "start": "npm --prefix backend exec prisma db push && npm --prefix backend start"
  }
}
